apiVersion: v1
kind: Service
metadata:
  name: mysql
  namespace: wordpress-ns
  labels:
    app: wordpress
    tier: mysql
spec:
  type: ClusterIP
  ports:
  - port: 3306
    targetPort: 3306
    protocol: TCP
  selector:
    app: wordpress
    tier: mysql
