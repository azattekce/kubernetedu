apiVersion: v1
kind: Secret
metadata:
  name: product-service-secrets
  namespace: default
type: Opaque
stringData:
  # Database credentials (Minikube MSSQL)
  database_user: "sa"
  database_password: "Az.123456+"

  # JWT secret
  jwt_secret_key: "your-super-secret-jwt-key-change-this-in-production"

  # Redis password (if needed)
  redis_password: ""
---
# Note: In production, use external secret management like:
# - Azure Key Vault
# - AWS Secrets Manager
# - HashiCorp Vault
# - Sealed Secrets
# Never commit actual secrets to version control!
